# Frozen truth is hashed byte-for-byte by tests (FROZEN_TRUTH_SHA256);
# line-ending conversion would break clean checkouts on Windows.
src/agentharness/procurement/eval_truth.json -text

# Web sources and the determinism-checked bundle must normalize to LF so
# Linux and Windows builds produce byte-identical output.
web/** text eol=lf
src/agentharness/web_dist/** text eol=lf
